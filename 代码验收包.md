# 代码验收包

## 当前结论

- V4.1 页面已生成：通过。
- `/setup/` 页面存在：通过。
- `/boss/` 页面存在：通过。
- 配置状态只显示 boolean：通过。
- 未在 HTML / JSON / 文档中写入真实 secret：通过。

## 页面级验收

- 首页只保留清晰首屏、3 件事和两个主按钮。
- `/setup/` 提供 5 步配置向导。
- `/boss/` 是老板 30 秒摘要，不包含完整日报、搜索、归档、配置说明。
- `/executive/` 保留经营摘要，包含今日结论、MQL、组织缺口、内容实验、协同事项、今日动作。
- MQL 看板包含开口数、MQL 数、转化率、CPL / MQL 成本、有效 MQL、客户画像完整率、销售反馈回填率、无效原因 Top 3、上周同日对比、链路版本。
- 手机端首页折叠次级模块，首屏只保留关键入口。

## 配置向导验收

- `/setup/` 页面存在。
- 配置状态只显示“已配置 / 缺失 / true / false”，不显示 secret。
- GitHub Secrets 链接可点击。
- GitHub Variables 链接可点击。
- Copy buttons 可用。
- Toast 可用。
- Run workflow 链接可点击。
- Demo warning 存在。
- Boss page 存在。
- Mobile setup page 可用。
- 录屏已生成。
- zip 已验证。

## 安全验收

- API Key 不在聊天、HTML、JSON、README、artifact、飞书卡片中展示。
- GitHub Pages 页面包含 noindex。
- robots.txt 禁止抓取。
- 加密模式下完整日报和搜索索引使用客户端解密。
- 页面明确提示：GitHub Pages 不是企业级登录系统。

## 自动化验收项

- 类型检查：通过。
- 构建：通过。
- 单元测试：通过。
- 全页面截图：应包含 V4.1 指定清单。
- 局部截图：应包含 V4.1 指定清单。
- 操作录屏：`desktop-30s-operator-flow.mp4`。
- 配置录屏：`desktop-30s-setup-flow.mp4`。
- 交付包：`YQN_Daily_Intelligence_Portal_V4_1_Delivery.zip`。

## 剩余技术风险

- 真实 MQL 成本、销售反馈率仍未接入真实业务系统。
- GitHub Secrets 无法被前端读取真实值，只能通过构建时 boolean 状态呈现。
- OpenAI API 费用由用户自己的 OpenAI API 账号承担。
